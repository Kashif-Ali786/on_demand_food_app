import 'package:flutter/material.dart';

//global navigator key is used if context is not passed down and is not available in any widget
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
